# 图文Agent使用指南

**创建时间**：2026年2月8日
**最后更新**：2026年2月8日
**版本**：v1.0

---

## 一、简介

图文Agent是一个基于华为云Gitee AI的智能配图生成工具，专为公众号内容创作者设计。它能够根据文章主题自动生成高质量的配图，支持多种风格选择，并严格遵守每日100张的使用限制。

本Agent的主要特点包括以下几个方面。首先是智能生成能力，它能够理解文章内容并生成匹配的配图，让配图与文章主题高度相关。其次是灵活的风格选择，用户可以根据文章类型选择科技、生活、商业等不同风格的配图。第三是使用量监控，系统会自动跟踪每日使用量，确保不超出100张的限制。第四是高质量输出，支持1024×1024高清分辨率，满足公众号配图的专业需求。

---

## 二、安装配置

### 2.1 环境准备

在开始使用图文Agent之前，您需要确保系统满足以下环境要求。操作系统方面，本Agent支持Windows、macOS和Linux系统。Python版本要求3.8或更高版本，建议使用Python 3.10以获得最佳兼容性。网络环境方面，需要能够访问华为云Gitee AI的API端点。

### 2.2 安装依赖库

创建虚拟环境后，使用pip命令安装所需的依赖库：

```bash
# 创建虚拟环境（可选但推荐）
python -m venv image_agent_env
source image_agent_env/bin/activate  # Linux/macOS
# 或
image_agent_env\Scripts\activate  # Windows

# 安装依赖
cd /Users/at_ol/.openclaw/workspace/skills/image-agent
pip install -r requirements.txt
```

requirements.txt文件包含以下核心依赖。openai库用于调用兼容OpenAI格式的API。requests库用于处理HTTP请求和图片下载。Pillow库提供图片处理功能（可选但推荐）。python-dotenv库用于环境变量管理（可选但推荐）。

### 2.3 API配置

图文Agent支持三种API配置方式，按照优先级从高到低排列。

第一种是直接在代码中传入API密钥，适用于快速测试场景，代码示例如下：

```python
from image_agent import ImageAgent

agent = ImageAgent(
    api_key="您的API密钥",
    base_url="https://ai.gitee.com/v1",
    daily_limit=100
)
```

第二种是通过环境变量配置，适用于正式使用场景。在系统环境变量或.env文件中设置以下变量：

```bash
# 设置环境变量（临时生效）
export GITEE_AI_API_KEY="your_api_key_here"
export GITEE_AI_BASE_URL="https://ai.gitee.com/v1"

# 或使用.env文件（推荐）
echo 'GITEE_AI_API_KEY="your_api_key_here"' > .env
echo 'GITEE_AI_BASE_URL="https://ai.gitee.com/v1"' >> .env
```

**⚠️ 安全提示**:
- ✅ 将 `.env` 文件加入 `.gitignore`
- ❌ 不要将 `.env` 文件推送到GitHub
- ✅ 定期更换API密钥

第三种是使用默认配置，适用于快速上手场景。如果不传入API密钥，Agent会从环境变量 `GITEE_AI_API_KEY` 读取密钥，或提示配置API密钥。

```python
# Agent会按以下顺序查找API密钥
# 1. 传入的 api_key 参数
# 2. 环境变量 GITEE_AI_API_KEY
# 3. 提示用户配置
```

---

## 三、快速开始

### 3.1 基础使用示例

以下是一个最基本的图片生成示例，演示如何生成一张简单的配图：

```python
from image_agent import ImageAgent, ImageStyle

# 初始化Agent（会自动使用默认API配置）
agent = ImageAgent()

# 生成一张图片
result = agent.generate(
    prompt="一只橙色的小猫咪趴在窗台上，可爱风格",
    size="1024x1024"
)

# 检查结果
if result.status == "success":
    print(f"生成成功！剩余配额：{result.remaining_quota}张")
    print(f"图片已保存：{result.images[0]['filename']}")
else:
    print(f"生成失败：{result.message}")
```

运行上述代码后，您将看到类似以下的输出信息。系统会显示生成过程的状态信息，包括提示词内容、生成进度等。如果生成成功，会显示图片保存路径和剩余配额。如果生成失败，会显示具体的错误信息。

### 3.2 指定风格生成

为不同类型的文章选择合适的配图风格，可以显著提升视觉效果。以下是各种风格的使用示例：

```python
from image_agent import ImageAgent, ImageStyle

agent = ImageAgent()

# 科技类文章配图
tech_result = agent.generate(
    prompt="人工智能和机器学习概念图",
    style=ImageStyle.TECH
)

# 商业财经类配图
business_result = agent.generate(
    prompt="现代化办公室环境和商务会议",
    style=ImageStyle.BUSINESS
)

# 生活方式类配图
lifestyle_result = agent.generate(
    prompt="温馨的家庭客厅和舒适的生活氛围",
    style=ImageStyle.LIFESTYLE
)

# 艺术创意类配图
artistic_result = agent.generate(
    prompt="抽象艺术和创意设计元素",
    style=ImageStyle.ARTISTIC
)

# 通用类配图
general_result = agent.generate(
    prompt="高质量的精美插画",
    style=ImageStyle.GENERAL
)
```

### 3.3 批量生成

当需要为多篇文章或一个系列生成配图时，可以使用批量生成功能：

```python
from image_agent import ImageAgent

agent = ImageAgent()

# 准备多个提示词
prompts = [
    "AI Agent成为2026年科技圈热点",
    "数字化转型的未来趋势",
    "智能办公新时代",
    "云计算与边缘计算融合",
    "数据驱动的决策制定"
]

# 批量生成（每次请求间隔1秒，避免触发速率限制）
results = agent.generate_batch(
    prompts=prompts,
    delay=1.0,  # 每次请求间隔1秒
    style=ImageStyle.TECH
)

# 处理结果
for i, result in enumerate(results):
    if result.status == "success":
        print(f"第{i+1}张生成成功：{result.images[0]['filename']}")
    else:
        print(f"第{i+1}张生成失败：{result.message}")

# 查看最终使用状态
status = agent.get_usage_status()
print(f"\n当前使用状态：{status['message']}")
```

---

## 四、进阶功能

### 4.1 提示词优化

图文Agent提供了提示词优化功能，可以根据文章内容自动生成更有效的提示词：

```python
from image_agent import ImageAgent, ImageStyle

agent = ImageAgent()

# 输入文章主题或摘要
article_topic = "2026年AI Agent最新趋势：这5个变化正在改变一切"

# 自动优化提示词
optimized_prompt = agent._optimize_prompt(
    context=article_topic,
    style=ImageStyle.TECH
)

print("原始主题：", article_topic)
print("优化提示词：", optimized_prompt)

# 使用优化后的提示词生成图片
result = agent.generate(prompt=optimized_prompt)
```

优化后的提示词会自动添加以下元素：风格相关的描述词汇、质量提升标签（如4K分辨率、锐利焦点等）、光照和色彩描述。这种优化可以显著提高生成图片的质量和相关性。

### 4.2 参数调优

您可以根据需要调整生成参数，以获得不同效果的图片：

```python
from image_agent import ImageAgent

agent = ImageAgent()

# 高质量模式（更精细的生成）
high_quality_result = agent.generate(
    prompt="一只可爱的猫咪",
    size="1024x1024",
    guidance_scale=7.5,       # 提高提示词相关性（范围1-20，推荐7-12）
    num_inference_steps=50,   # 增加推理步数（范围1-100，推荐30-50）
    save=True
)

# 快速模式（较快的生成速度）
fast_result = agent.generate(
    prompt="一只可爱的猫咪",
    size="512x512",           # 减小尺寸可加快生成速度
    guidance_scale=3,         # 降低相关性要求
    num_inference_steps=20,   # 减少推理步数
    save=True
)
```

参数调整说明如下。size参数控制图片尺寸，可选值包括256×256、512×512、1024×1024等，尺寸越大生成时间越长。guidance_scale参数控制提示词相关性，数值越高生成图片越符合提示词，但过高可能导致图片过于生硬，推荐范围为5至10。num_inference_steps参数控制生成质量，数值越高图片质量越好，但生成时间越长，推荐范围为30至50。

### 4.3 使用状态监控

图文Agent提供了完整的使用量监控功能，帮助您跟踪每日使用情况：

```python
from image_agent import ImageAgent

agent = ImageAgent()

# 获取当前使用状态
status = agent.get_usage_status()

print("=" * 50)
print("图文Agent使用状态")
print("=" * 50)
print(f"状态等级：{status['level']}")
print(f"状态信息：{status['message']}")
print(f"已使用：{status['used']}张")
print(f"剩余配额：{status['remaining']}张")
print(f"重置日期：{status['reset_time']}")
print("=" * 50)

# 根据状态做出相应处理
if status['level'] == 'critical':
    print("⚠️ 已达上限，请明天再试")
elif status['level'] == 'warning':
    print("⚠️ 配额不足20%，请注意使用")
else:
    print("✅ 配额充足，可正常使用")
```

监控状态分为三个等级。正常状态表示剩余配额超过80%，一切正常。警告状态表示剩余配额低于20%，需要关注使用量。严重状态表示配额已用尽，需要等待第二天重置。

---

## 五、完整工作流

### 5.1 公众号配图生成流程

以下是一个完整的公众号配图生成工作流，演示如何为公众号文章生成配套配图：

```python
from image_agent import ImageAgent, ImageStyle
import os

class WeChatPublicAccountWorkflow:
    """公众号配图生成工作流"""
    
    def __init__(self):
        self.agent = ImageAgent(
            daily_limit=100,
            config=None  # 使用默认配置
        )
        self.output_dir = "./wechat_images"
        os.makedirs(self.output_dir, exist_ok=True)
    
    def generate_cover_image(self, article_title: str, style: str = "tech") -> dict:
        """生成文章封面图"""
        # 根据文章标题生成封面提示词
        prompt = f"公众号文章封面，主题：{article_title}，专业设计，高质量"
        
        # 根据风格选择对应的ImageStyle
        style_map = {
            "tech": ImageStyle.TECH,
            "business": ImageStyle.BUSINESS,
            "lifestyle": ImageStyle.LIFESTYLE,
            "artistic": ImageStyle.ARTISTIC,
            "general": ImageStyle.GENERAL
        }
        
        return self.agent.generate(
            prompt=prompt,
            size="1024x1024",
            style=style_map.get(style, ImageStyle.GENERAL),
            save=True
        )
    
    def generate_article_images(self, article_sections: list) -> list:
        """生成文章内嵌配图"""
        results = []
        
        for i, section in enumerate(article_sections):
            result = self.agent.generate(
                prompt=f"配图：{section}，公众号风格，简洁明了",
                size="800x800",
                style=ImageStyle.GENERAL,
                save=True
            )
            results.append({
                "section": section,
                "result": result
            })
        
        return results
    
    def run(self, article_info: dict) -> dict:
        """运行完整工作流"""
        print("=" * 60)
        print("公众号配图生成工作流")
        print("=" * 60)
        
        report = {
            "article_title": article_info.get("title", ""),
            "cover_image": None,
            "section_images": [],
            "total_generated": 0,
            "remaining_quota": 0
        }
        
        # 1. 生成封面图
        print(f"\n📰 文章标题：{article_info['title']}")
        print("🖼️  生成封面图...")
        
        cover_result = self.generate_cover_image(
            article_title=article_info['title'],
            style=article_info.get('style', 'tech')
        )
        
        if cover_result.status == "success":
            print(f"✅ 封面生成成功：{cover_result.images[0]['filename']}")
            report['cover_image'] = cover_result.images[0]['filename']
            report['total_generated'] += 1
        else:
            print(f"❌ 封面生成失败：{cover_result.message}")
        
        # 2. 生成文章内嵌配图
        if article_info.get('sections'):
            print(f"\n📝 生成{len(article_info['sections'])}张内嵌配图...")
            
            section_results = self.generate_article_images(article_info['sections'])
            
            for i, item in enumerate(section_results):
                if item['result'].status == "success":
                    print(f"✅ 第{i+1}张内嵌配图成功：{item['result'].images[0]['filename']}")
                    report['section_images'].append(item['result'].images[0]['filename'])
                    report['total_generated'] += 1
                else:
                    print(f"❌ 第{i+1}张内嵌配图失败：{item['result'].message}")
        
        # 3. 输出最终报告
        report['remaining_quota'] = self.agent.get_usage_status()['remaining']
        
        print("\n" + "=" * 60)
        print("工作流完成报告")
        print("=" * 60)
        print(f"文章标题：{report['article_title']}")
        print(f"封面图：{report['cover_image']}")
        print(f"内嵌配图：{len(report['section_images'])}张")
        print(f"总计生成：{report['total_generated']}张")
        print(f"剩余配额：{report['remaining_quota']}张")
        print("=" * 60)
        
        return report

# 使用工作流
workflow = WeChatPublicAccountWorkflow()

article_info = {
    "title": "2026年AI Agent最新趋势：这5个变化正在改变一切",
    "style": "tech",
    "sections": [
        "从聊天到干活：AI Agent成为智能助手",
        "每个行业都在重新定义员工",
        "Personal AI Agent成为新数字资产",
        "监管加速，合规将成为核心竞争力"
    ]
}

result = workflow.run(article_info)
```

### 5.2 集成到现有系统

如果您已有内容管理系统，可以将图文Agent集成到现有系统中：

```python
from image_agent import ImageAgent, ImageStyle
from flask import Flask, request, jsonify

app = Flask(__name__)
agent = ImageAgent()

@app.route('/api/generate-image', methods=['POST'])
def generate_image():
    """图片生成API端点"""
    data = request.json
    
    # 验证请求
    if not data or 'prompt' not in data:
        return jsonify({
            "status": "error",
            "message": "缺少prompt参数"
        }), 400
    
    # 检查使用量
    status = agent.get_usage_status()
    if status['level'] == 'critical':
        return jsonify({
            "status": "error",
            "message": "已达每日上限，请明天再试",
            "remaining": 0
        }), 429
    
    # 生成图片
    result = agent.generate(
        prompt=data['prompt'],
        size=data.get('size', '1024x1024'),
        style=ImageStyle(data.get('style', 'general'))
    )
    
    return jsonify({
        "status": result.status,
        "message": result.message,
        "remaining_quota": result.remaining_quota,
        "images": result.images
    })

@app.route('/api/usage-status', methods=['GET'])
def usage_status():
    """使用状态查询API端点"""
    return jsonify(agent.get_usage_status())

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)
```

---

## 六、常见问题

### 6.1 生成相关问题

**问题一：生成的图片与提示词不符**

如果生成的图片与您的预期有较大差异，可以尝试以下解决方案。首先，增加guidance_scale参数的值，推荐设置为7至10。其次，尝试更详细、具体的提示词，描述清楚您想要的内容、风格、色调等。第三，使用特定的风格参数，如ImageStyle.TECH或ImageStyle.BUSINESS。

**问题二：生成速度慢**

生成速度受多个因素影响。图片尺寸越大，生成时间越长，建议使用1024×1024以内的尺寸。推理步数越多，质量越好但速度越慢，建议使用30至50步。使用共享网络可能会影响速度，建议在网络较好的环境下使用。

**问题三：图片质量不佳**

提高图片质量的方法包括以下几点。增加num_inference_steps参数，推荐设置为50至100。在提示词中添加质量描述词，如4K、high quality、detailed等。使用更具体的提示词，描述您期望的画面细节。

### 6.2 使用限制问题

**问题四：遇到速率限制错误（HTTP 429）**

如果遇到速率限制错误，说明您的请求过于频繁。解决方法包括降低请求频率，在两次请求之间添加至少1秒的延迟。实施指数退避策略，如果遇到429错误，等待1秒后重试，如果还是429，等待2秒，然后是4秒等。减少并发请求，避免同时发送多个请求。

**问题五：已达每日上限**

图文Agent每日限制100张图片。解决方法包括查看剩余配额状态，合理规划使用。检查是否有未及时清理的已生成图片，这些也计入每日限额。等待UTC时间0点重置，通常是北京时间早上8点。

### 6.3 技术问题

**问题六：API认证失败**

如果遇到API认证失败错误，首先检查API密钥是否正确，可以确认密钥是否包含正确的字符。其次验证API密钥是否有效，可以尝试重新获取或确认账户状态。第三检查base_url是否正确，确保使用华为云Gitee AI的正确端点。

**问题七：图片无法保存**

如果图片无法保存到本地，请检查目标目录是否存在并有写入权限。确保磁盘空间充足。如果使用相对路径，请确认当前工作目录是否正确。

---

## 七、最佳实践

### 7.1 提示词优化技巧

在使用图文Agent生成图片时，提示词的质量直接影响生成效果。以下是一些实用的提示词优化技巧。

首先是明确主体。在提示词开头明确描述图片的主体内容，例如"一只橙色的小猫"比"可爱的动物"更具体。

其次是添加风格描述。根据文章类型添加相应的风格描述，如"科技感"、"商务风格"、"温馨氛围"等。

第三是使用质量标签。添加质量相关的描述词可以提升生成质量，如"高质量"、"4K分辨率"、"专业摄影"等。

第四是避免过于复杂。提示词不宜过长或过于复杂，聚焦于最核心的描述，避免过多相互冲突的要求。

### 7.2 效率提升建议

为了更高效地使用图文Agent，建议采取以下做法。

第一是建立提示词模板库。将常用的提示词模式保存为模板，方便快速调用。例如科技类文章可以建立通用模板，只需替换具体主题。

第二是批量生成。对于需要多张配图的文章，建议使用批量生成功能，并设置合理的请求间隔（建议1秒）。

第三是及时保存。生成的图片建议及时保存到云端或本地，图文Agent默认会保存到本地generated_images目录。

第四是定期清理。定期清理不需要的已生成图片，释放存储空间。

### 7.3 成本控制建议

虽然图文Agent有每日100张的限制，但合理的使用策略可以帮助您最大化利用配额。

第一是优先保证重要文章。将配额度量留给最重要的文章封面或核心配图。

第二是利用免费资源。对于非核心配图，可以考虑使用免费图库或其他免费工具。

第三是合理规划。提前规划每周需要生成的文章配图数量，避免临时需要时配额不足。

第四是监控使用。定期检查使用状态，在配额即将用尽前合理安排使用。

---

## 八、故障排除

### 8.1 错误代码说明

图文Agent可能返回以下错误代码和对应的解决方案。

当返回错误代码401时，表示API认证失败，请检查API密钥是否正确、是否过期。

当返回错误代码429时，表示请求过于频繁，请降低请求频率或实施指数退避策略。

当返回错误代码400时，表示请求参数错误，请检查参数格式是否正确、大小是否在支持范围内。

当返回错误代码500时，表示服务器内部错误，请稍后重试或联系技术支持。

### 8.2 调试模式

如果遇到问题，可以启用调试模式获取更多信息：

```python
import logging

# 启用详细日志
logging.basicConfig(level=logging.DEBUG)

from image_agent import ImageAgent

agent = ImageAgent()

# 生成的详细信息会打印到控制台
result = agent.generate(prompt="测试图片")
```

### 8.3 重试机制

建议在生产环境中实现重试机制：

```python
import time
from image_agent import ImageAgent, GenerationResult

def generate_with_retry(agent, prompt, max_retries=3) -> GenerationResult:
    """带重试机制的图片生成"""
    for attempt in range(max_retries):
        try:
            result = agent.generate(prompt)
            
            if result.status == "success":
                return result
            
            # 如果是速率限制（429），实施指数退避
            if "429" in result.message or "too many" in result.message.lower():
                wait_time = (2 ** attempt)
                print(f"速率限制，等待{wait_time}秒后重试...")
                time.sleep(wait_time)
                continue
            
            # 其他错误，直接返回
            return result
            
        except Exception as e:
            if attempt < max_retries - 1:
                wait_time = (2 ** attempt)
                time.sleep(wait_time)
            else:
                raise e
    
    return GenerationResult(
        status="error",
        message="超过最大重试次数",
        remaining_quota=0
    )

# 使用
agent = ImageAgent()
result = generate_with_retry(agent, "测试图片")
```

---

## 九、参考资源

### 9.1 相关文档

图文Agent的完整文档体系包括以下几个部分。《图文Agent知识库》（SKILL.md）提供了Agent的详细功能说明和配置指南。《NVIDIA API使用规则》（memory/nvidia-api-rules.md）提供了API调用的速率限制和最佳实践说明。《免费图片生成API汇总》（memory/free-image-generation-apis.md）提供了其他免费图片生成方案的对比和选择建议。

### 9.2 技术支持

如果您在使用过程中遇到问题，可以通过以下渠道获取帮助。首先可以查阅本文档的常见问题部分。其次可以在GitHub上提交Issue获取技术支持。最后可以联系系统管理员获取进一步帮助。

### 9.3 更新日志

版本1.0（2026年2月8日）的更新内容包括初始版本发布、基础图片生成功能实现、多种风格支持、每日使用量监控以及批量生成功能支持。

---

**文档更新时间**：2026年2月8日
**维护者**：OpenClaw Agent
**联系方式**：通过工作会话联系
